server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
         server-addr: localhost:8848 #配置Nacos地址
        #server-addr:  62.234.132.219:1111 #配置Nacos集群地址

management:
  endpoints:
    web:
      exposure:
        include: '*'  #监控
